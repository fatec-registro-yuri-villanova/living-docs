---
sidebar_position: 15
title: "Aula 15: Leaderboard e Rankings"
description: "Tela de ranking com FlatList, ordenação por XP e destaque do usuário logado."
---

# Aula 15: Leaderboard e Rankings

:::info 🔀 Branch da Aula
Esta aula refere-se à branch: **`aula-15`**
```bash
git checkout aula-15
```
:::

:::caution 🚧 Conteúdo Prospectivo
Esta branch ainda não possui código implementado.
:::

## 📖 Conceito

O **Leaderboard** exibe os alunos ranqueados por XP. O usuário logado é destacado visualmente.

## 💻 Código de Referência

```tsx title="app/leaderboard.tsx"
interface RankedUser {
    id: string;
    name: string;
    xp: number;
    position: number;
    avatarUrl?: string;
}

function LeaderboardItem({ item, isCurrentUser }: {
    item: RankedUser; isCurrentUser: boolean
}) {
    return (
        <View style={[styles.row, isCurrentUser && styles.highlight]}>
            <Text style={styles.position}>#{item.position}</Text>
            <Text style={styles.name}>{item.name}</Text>
            <Text style={styles.xp}>{item.xp} XP</Text>
        </View>
    );
}
```

## 💻 Mão na Massa

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs>
  <TabItem value="ex1" label="Exercício 1 — Fixação" default>

Busque `/leaderboard` da API e exiba com `FlatList`.

  </TabItem>
  <TabItem value="ex2" label="Exercício 2 — Aplicação">

Destaque o top 3 com cores dourado, prata e bronze. Adicione ícones de medalha.

  </TabItem>
  <TabItem value="ex3" label="Exercício 3 — Desafio">

Adicione filtro por período (semanal/mensal/geral) com `SegmentedControl`.

  </TabItem>
</Tabs>
